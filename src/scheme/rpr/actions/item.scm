;; auto-generated file. see gen.scm
(ns rpr.actions.item
  :require ((rpr) (rpr.common)))
(define cmd rpr.common/cmd) ;; runs either native actions (with number id) or named actions (eg SWS)

(define (remove-items) (cmd 40006))
(define (split-items-at-edit-or-play-cursor) (cmd 40012))
(define (copy-loop-of-selected-area-of-audio-items) (cmd 40014))
(define (paste-items-tracks-<old-style-handling-of-hidden-tracks>) (cmd 40058))
(define (copy-selected-area-of-items) (cmd 40060))
(define (split-items-at-time-selection) (cmd 40061))
(define (set-all-media-offline) (cmd 40100))
(define (set-all-media-online) (cmd 40101))
(define (open-items-in-primary-external-editor) (cmd 40109))
(define (open-item-copies-in-primary-external-editor) (cmd 40132))
(define (open-in-built-in-midi-editor-<set-default-behavior-in-preferences>) (cmd 40153))
(define (select-all-items) (cmd 40182))
(define (split-items-at-edit-or-play-cursor-<ignoring-grouping>) (cmd 40186))
(define (split-items-at-play-cursor) (cmd 40196))
(define (open-items-in-secondary-external-editor) (cmd 40202))
(define (open-item-copies-in-secondary-external-editor) (cmd 40203))
(define (apply-track-take-fx-to-items) (cmd 40209))
(define (glue-items,-ignoring-time-selection,-including-leading-fade-in-and-trailing-fade-out) (cmd 40257))
(define (reverse-items-to-new-take) (cmd 40270))
(define (unselect-all-items) (cmd 40289))
(define (move-to-source-preferred-position-<used-by-bwf>) (cmd 40299))
(define (cut-selected-area-of-items) (cmd 40307))
(define (remove-selected-area-of-items) (cmd 40312))
(define (auto-trim-split-items-<remove-silence>...) (cmd 40315))
(define (quantize-item-positions-to-grid...) (cmd 40316))
(define (apply-track-take-fx-to-items-<mono-output>) (cmd 40361))
(define (glue-items,-ignoring-time-selection) (cmd 40362))
(define (select-all-items-in-track) (cmd 40421))
(define (apply-track-take-fx-to-items-<midi-output>) (cmd 40436))
(define (set-selected-media-online) (cmd 40439))
(define (set-selected-media-temporarily-offline) (cmd 40440))
(define (trim-items-to-selected-area) (cmd 40508))
(define (fade-items-in-to-cursor) (cmd 40509))
(define (fade-items-out-from-cursor) (cmd 40510))
(define (trim-items-left-of-cursor) (cmd 40511))
(define (trim-items-right-of-cursor) (cmd 40512))
(define (select-item-under-mouse-cursor) (cmd 40528))
(define (select-item-under-mouse-cursor-<leaving-other-items-selected>) (cmd 40529))
(define (toggle-selection-of-item-under-mouse-cursor) (cmd 40530))
(define (set-snap-offset-to-cursor) (cmd 40541))
(define (set-snap-offset-to-nearest-grid-line) (cmd 40542))
(define (heal-splits-in-items) (cmd 40548))
(define (render-items-to-new-take-<preserve-source-type>) (cmd 40601))
(define (glue-items,-including-leading-fade-in-and-trailing-fade-out) (cmd 40606))
(define (set-item-end-to-cursor) (cmd 40611))
(define (set-item-end-to-source-media-end) (cmd 40612))
(define (show-fx-chain-for-item-take) (cmd 40638))
(define (remove-fx-for-item-take) (cmd 40640))
(define (implode-items-across-tracks-into-items-on-one-track) (cmd 40644))
(define (auto-reposition-items-in-free-item-positioning-mode) (cmd 40645))
(define (import-item-media-cues-as-project-markers) (cmd 40692))
(define (set-to-custom-color...) (cmd 40704))
(define (set-to-random-colors) (cmd 40705))
(define (set-to-one-random-color) (cmd 40706))
(define (set-to-default-color) (cmd 40707))
(define (select-all-items-in-current-time-selection) (cmd 40717))
(define (select-all-items-on-selected-tracks-in-current-time-selection) (cmd 40718))
(define (split-at-media-cues) (cmd 40732))
(define (shrink-to-first-media-cue) (cmd 40733))
(define (shrink-to-last-media-cue) (cmd 40734))
(define (shrink-to-first-and-last-media-cues) (cmd 40735))
(define (split-item-under-mouse-cursor) (cmd 40746))
(define (split-item-under-mouse-cursor-<select-left>) (cmd 40747))
(define (split-item-under-mouse-cursor-<select-right>) (cmd 40748))
(define (split-items-at-edit-cursor-<no-change-selection>) (cmd 40757))
(define (split-items-at-edit-cursor-<select-left>) (cmd 40758))
(define (split-items-at-edit-cursor-<select-right>) (cmd 40759))
(define (dynamic-split-items...) (cmd 40760))
(define (create-chromatic-midi-from-items) (cmd 40773))
(define (split-at-previous-zero-crossing) (cmd 40792))
(define (open-item-inline-editors) (cmd 40847))
(define (show-notes-for-items...) (cmd 40850))
(define (mute-active-take-of-multitake-item-within-time-selection) (cmd 40855))
(define (solo-active-take-of-multitake-item-within-time-selection) (cmd 40856))
(define (explode-multichannel-audio-or-midi-to-new-one-channel-items) (cmd 40894))
(define (set-focus-to-item-under-mouse-cursor) (cmd 40911))
(define (crossfade-items-within-time-selection) (cmd 40916))
(define (set-item-mix-behavior-to-enclosed-items-replace-enclosing-items) (cmd 40918))
(define (set-item-mix-behavior-to-always-mix) (cmd 40919))
(define (explode-midi-note-rows-<pitch>-to-new-items) (cmd 40920))
(define (set-item-mix-behavior-to-always-replace) (cmd 40921))
(define (set-item-mix-behavior-to-project-default) (cmd 40922))
(define (remove-content-<trim>-behind-items) (cmd 40930))
(define (split-items-at-project-markers) (cmd 40931))
(define (split-items-at-timeline-grid) (cmd 40932))
(define (crossfade-any-overlapping-items) (cmd 41059))
(define (move,-stretch,-and-loop-items-to-fit-time-selection) (cmd 41069))
(define (paste-items-tracks,-creating-pooled-<ghost>-midi-items-and-automation-items-regardless-of-preferences-media-midi-and-preferences-media-automation-settings) (cmd 41072))
(define (invert-selection) (cmd 41115))
(define (select-next-adjacent-non-overlapping-item) (cmd 41127))
(define (select-previous-adjacent-non-overlapping-item) (cmd 41128))
(define (snap-items-left) (cmd 41182))
(define (snap-items-right) (cmd 41183))
(define (snap-items-to-nearest-snap-point) (cmd 41184))
(define (remove-fade-in) (cmd 41191))
(define (remove-fade-out) (cmd 41192))
(define (remove-fade-in-and-fade-out) (cmd 41193))
(define (toggle-enable-disable-default-fadein-fadeout) (cmd 41194))
(define (enable-default-fadein-fadeout) (cmd 41195))
(define (disable-default-fadein-fadeout) (cmd 41196))
(define (move-and-stretch-items-to-fit-time-selection) (cmd 41206))
(define (paste-items-tracks-at-mouse-position) (cmd 41221))
(define (duplicate-items) (cmd 41295))
(define (duplicate-selected-area-of-items) (cmd 41296))
(define (copy-items-to-time-selection,-trim-loop-to-fit) (cmd 41319))
(define (move-items-to-time-selection,-trim-loop-to-fit) (cmd 41320))
(define (remove-all-empty-take-lanes) (cmd 41348))
(define (remove-the-empty-take-lane-before-the-active-take) (cmd 41349))
(define (remove-the-empty-take-lane-after-the-active-take) (cmd 41350))
(define (add-an-empty-take-lane-before-the-active-take) (cmd 41351))
(define (add-an-empty-take-lane-after-the-active-take) (cmd 41352))
(define (rotate-take-lanes-forward) (cmd 41353))
(define (rotate-take-lanes-backward) (cmd 41354))
(define (move-active-takes-to-top-lane) (cmd 41380))
(define (choose-active-take-for-item-under-mouse) (cmd 41381))
(define (fit-items-to-time-selection,-padding-with-silence-if-needed) (cmd 41385))
(define (fit-items-to-time-selection,-looping-if-needed) (cmd 41386))
(define (explode-rex-item-into-beat-slices) (cmd 41513))
(define (cycle-through-fade-in-shapes) (cmd 41520))
(define (cycle-through-fade-out-shapes) (cmd 41527))
(define (cycle-through-crossfade-shapes) (cmd 41534))
(define (glue-items) (cmd 41588))
(define (select-all-other-media-items-that-share-pooled-<ghost>-midi-source-data-with-selected-items) (cmd 41611))
(define (remove-active-take-from-midi-source-data-pool-<aka-un-pool,-un-ghost,-make-unique>) (cmd 41613))
(define (reorder-adjacent-items-randomly) (cmd 41638))
(define (set-item-ends-to-start-of-next-item) (cmd 41639))
(define (collapse-empty-take-lanes) (cmd 41747))
(define (insert-time-on-tracks-and-paste-items) (cmd 41748))
(define (open-associated-project-in-new-tab) (cmd 41816))
(define (split-items-at-end-of-fade-in-<unless-crossfaded>) (cmd 41839))
(define (split-items-at-start-of-fade-out-<unless-crossfaded>) (cmd 41840))
(define (add-stretch-marker-at-cursor) (cmd 41842))
(define (add-stretch-markers-at-time-selection) (cmd 41843))
(define (remove-all-stretch-markers) (cmd 41844))
(define (remove-all-stretch-markers-in-time-selection) (cmd 41845))
(define (snap-stretch-markers-to-grid) (cmd 41846))
(define (snap-stretch-markers-in-time-selection-to-grid) (cmd 41847))
(define (add-stretch-marker-at-mouse-position) (cmd 41848))
(define (force-tonal-optimized-mode-for-stretch-markers) (cmd 41857))
(define (set-item-name-from-active-take-filename) (cmd 41858))
(define (remove-stretch-marker-at-current-position) (cmd 41859))
(define (go-to-next-stretch-marker) (cmd 41860))
(define (go-to-previous-stretch-marker) (cmd 41861))
(define (go-to-nearest-stretch-marker) (cmd 41862))
(define (close-item-inline-editors) (cmd 41887))
(define (propagate-to-similarly-named-items-on-track) (cmd 41977))
(define (propagate-to-all-similarly-named-items) (cmd 41979))
(define (edit-stretch-marker-at-cursor) (cmd 41988))
(define (reset-stretch-marker-at-current-position) (cmd 41989))
(define (apply-track-take-fx-to-items-<multichannel-output>) (cmd 41993))
(define (move-items-to-subproject-<non-destructive-glue>) (cmd 41996))
(define (render-items-to-new-take) (cmd 41999))
(define (glue-items,-ignoring-time-selection-<auto-increase-channel-count-with-take-fx>) (cmd 42008))
(define (glue-items-<auto-increase-channel-count-with-take-fx>) (cmd 42009))
(define (set-item-start-end-to-source-media-start-end) (cmd 42228))
(define (set-item-start-to-source-media-start) (cmd 42229))
(define (force-transient-optimized-mode-for-stretch-markers) (cmd 42337))
(define (force-balanced-mode-for-stretch-markers) (cmd 42338))
(define (force-no-pre-echo-reduction-mode-for-stretch-markers) (cmd 42339))
(define (toggle-force-media-offline) (cmd 42356))
(define (toggle-force-inactive-take-media-offline) (cmd 42357))
(define (convert-embedded-source-transient-information-to-transient-guides) (cmd 42380))
(define (add-edit-take-marker-at-play-position-or-edit-cursor) (cmd 42385))
(define (delete-take-marker-at-cursor) (cmd 42386))
(define (delete-all-take-markers) (cmd 42387))
(define (add-edit-take-marker-at-mouse-position) (cmd 42388))
(define (delete-take-marker-at-mouse-position) (cmd 42389))
(define (quick-add-take-marker-at-play-position-or-edit-cursor) (cmd 42390))
(define (quick-add-take-marker-at-mouse-position) (cmd 42391))
(define (set-cursor-to-previous-take-marker-in-selected-items) (cmd 42393))
(define (set-cursor-to-next-take-marker-in-selected-items) (cmd 42394))
(define (paste-items-tracks) (cmd 42398))
