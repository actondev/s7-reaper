(ns rpr.gen.track
  :require ((rpr)))

(define cmd rpr/Main_OnCommand)

(define (set-pan-for-selected-tracks--midi-cc-osc-only-) (cmd 5))
(define (toggle-mute-for-selected-tracks) (cmd 6))
(define (toggle-solo-for-selected-tracks) (cmd 7))
(define (toggle-fx-bypass-for-selected-tracks) (cmd 8))
(define (toggle-record-arm-for-selected-tracks) (cmd 9))
(define (set-mute-for-selected-tracks--midi-cc-osc-only-) (cmd 10))
(define (set-solo-for-selected-tracks--midi-cc-osc-only-) (cmd 11))
(define (set-volume-for-master-track--midi-cc-osc-only-) (cmd 12))
(define (set-pan-for-master-track--midi-cc-osc-only-) (cmd 13))
(define (toggle-mute-for-master-track) (cmd 14))
(define (toggle-solo-for-master-track) (cmd 15))
(define (toggle-fx-bypass-for-master-track) (cmd 16))
(define (set-mute-for-master-track--midi-cc-osc-only-) (cmd 18))
(define (set-solo-for-master-track--midi-cc-osc-only-) (cmd 19))
(define (set-volume-for-last-touched-track--midi-cc-osc-only-) (cmd 812))
(define (set-pan-for-last-touched-track--midi-cc-osc-only-) (cmd 813))
(define (toggle-mute-for-last-touched-track) (cmd 814))
(define (toggle-solo-for-last-touched-track) (cmd 815))
(define (toggle-fx-bypass-for-last-touched-track) (cmd 816))
(define (toggle-record-arm-for-last-touched-track) (cmd 817))
(define (set-mute-for-last-touched-track--midi-cc-osc-only-) (cmd 818))
(define (set-solo-for-last-touched-track--midi-cc-osc-only-) (cmd 819))
(define (cycle-track-folder-state) (cmd 1041))
(define (cycle-folder-collapsed-state) (cmd 1042))
(define (set-stereo-width--or-right-channel-pan--for-selected-tracks--midi-cc-osc-only-) (cmd 30000))
(define (set-stereo-width--or-right-channel-pan--for-master-track--midi-cc-osc-only-) (cmd 30001))
(define (set-stereo-width--or-right-channel-pan--for-last-touched-track--midi-cc-osc-only-) (cmd 30101))
(define (insert-new-track) (cmd 40001))
(define (remove-tracks) (cmd 40005))
(define (toggle-track-pre-fx-volume-envelope-active) (cmd 40050))
(define (toggle-track-pre-fx-pan-envelope-active) (cmd 40051))
(define (toggle-track-volume-envelope-active) (cmd 40052))
(define (toggle-track-pan-envelope-active) (cmd 40053))
(define (duplicate-tracks) (cmd 40062))
(define (nudge-track-volume-up) (cmd 40115))
(define (nudge-track-volume-down) (cmd 40116))
(define (copy-tracks) (cmd 40210))
(define (toggle-show-hide-in-mixer) (cmd 40250))
(define (mute-unmute-tracks) (cmd 40280))
(define (solo-unsolo-tracks) (cmd 40281))
(define (invert-track-phase) (cmd 40282))
(define (nudge-track-pan-left) (cmd 40283))
(define (nudge-track-pan-right) (cmd 40284))
(define (go-to-next-track) (cmd 40285))
(define (go-to-previous-track) (cmd 40286))
(define (go-to-next-track--leaving-other-tracks-selected-) (cmd 40287))
(define (go-to-previous-track--leaving-other-tracks-selected-) (cmd 40288))
(define (view-fx-chain-for-current-last-touched-track) (cmd 40291))
(define (view-envelopes-for-current-last-touched-track) (cmd 40292))
(define (view-routing-and-i-o-for-current-last-touched-track) (cmd 40293))
(define (toggle-record-arming-for-current-last-touched-track) (cmd 40294))
(define (select-all-tracks) (cmd 40296))
(define (unselect-all-tracks) (cmd 40297))
(define (toggle-fx-bypass-for-current-last-touched-track) (cmd 40298))
(define (cut-tracks) (cmd 40337))
(define (unmute-all-tracks) (cmd 40339))
(define (unsolo-all-tracks) (cmd 40340))
(define (mute-all-tracks) (cmd 40341))
(define (bypass-fx-on-all-tracks) (cmd 40342))
(define (unbypass-fx-on-all-tracks) (cmd 40343))
(define (toggle-fx-bypass-on-all-tracks) (cmd 40344))
(define (set-to-custom-color...) (cmd 40357))
(define (set-to-random-colors) (cmd 40358))
(define (set-to-default-color) (cmd 40359))
(define (set-to-one-random-color) (cmd 40360))
(define (save-tracks-as-track-template...) (cmd 40392))
(define (render-tracks-to-stereo-post-fader-stem-tracks--and-mute-originals-) (cmd 40405))
(define (toggle-track-volume-envelope-visible) (cmd 40406))
(define (toggle-track-pan-envelope-visible) (cmd 40407))
(define (toggle-track-pre-fx-volume-envelope-visible) (cmd 40408))
(define (toggle-track-pre-fx-pan-envelope-visible) (cmd 40409))
(define (arm-all-tracks-for-recording) (cmd 40490))
(define (unarm-all-tracks-for-recording) (cmd 40491))
(define (set-track-record-monitor-to-off) (cmd 40492))
(define (set-track-record-monitor-to-on) (cmd 40493))
(define (set-track-record-monitor-to-auto-tape) (cmd 40494))
(define (cycle-track-record-monitor) (cmd 40495))
(define (set-track-record-mode-to-input) (cmd 40496))
(define (set-track-record-mode-to-output--stereo-) (cmd 40497))
(define (set-track-record-mode-to-none--monitoring-only-) (cmd 40498))
(define (set-track-record-mode-to-output--stereo-latency-compensated-) (cmd 40499))
(define (set-track-record-mode-to-midi-output) (cmd 40500))
(define (set-track-record-mode-to-output--mono-) (cmd 40501))
(define (set-track-record-mode-to-output--mono-latency-compensated-) (cmd 40502))
(define (set-track-record-mode-to-midi-overdub) (cmd 40503))
(define (set-track-record-mode-to-midi-replace) (cmd 40504))
(define (select-last-touched-track) (cmd 40505))
(define (set-all-fx-offline-for-selected-tracks) (cmd 40535))
(define (set-all-fx-online-for-selected-tracks) (cmd 40536))
(define (render-tracks-to-mono-post-fader-stem-tracks--and-mute-originals-) (cmd 40537))
(define (view-track-recording-settings--midi-quantize,-file-format-path--for-last-touched-track) (cmd 40604))
(define (allow-track-media-buffering) (cmd 40607))
(define (prevent-track-media-buffering) (cmd 40608))
(define (allow-track-anticipative-fx) (cmd 40609))
(define (prevent-track-anticipative-fx) (cmd 40610))
(define (rename-last-touched-track) (cmd 40696))
(define (insert-new-track-at-end-of-track-list) (cmd 40702))
(define (solo-tracks) (cmd 40728))
(define (unsolo-tracks) (cmd 40729))
(define (mute-tracks) (cmd 40730))
(define (unmute-tracks) (cmd 40731))
(define (toggle-automatic-record-arm-when-track-selected) (cmd 40736))
(define (set-automatic-record-arm-when-track-selected) (cmd 40737))
(define (clear-automatic-record-arm) (cmd 40738))
(define (set-clear-all-tracks-automatic-record-arm) (cmd 40740))
(define (nudge-master-track-volume-up) (cmd 40743))
(define (nudge-master-track-volume-down) (cmd 40744))
(define (unset-track-solo-defeat--all-tracks-) (cmd 40770))
(define (toggle-all-track-grouping-enabled) (cmd 40771))
(define (set-track-grouping-parameters) (cmd 40772))
(define (render-tracks-to-stereo-stem-tracks--and-mute-originals-) (cmd 40788))
(define (render-tracks-to-mono-stem-tracks--and-mute-originals-) (cmd 40789))
(define (view-input-fx-chain-for-current-last-touched-track) (cmd 40844))
(define (view-fx-chain-for-master-track) (cmd 40846))
(define (set-track-record-mode-to-midi-touch-replace) (cmd 40852))
(define (toggle-show-hide-in-tcp) (cmd 40853))
(define (toggle-track-mute-envelope-active) (cmd 40866))
(define (toggle-track-mute-envelope-visible) (cmd 40867))
(define (freeze-to-multichannel--render-pre-fader,-save-remove-items-and-online-fx-) (cmd 40877))
(define (render-tracks-to-multichannel-post-fader-stem-tracks--and-mute-originals-) (cmd 40892))
(define (render-tracks-to-multichannel-stem-tracks--and-mute-originals-) (cmd 40893))
(define (set-track-record-mode-to-output--full-multichannel-) (cmd 40895))
(define (set-track-record-mode-to-output--full-multichannel-compensated-) (cmd 40896))
(define (set-track-icon...) (cmd 40899))
(define (remove-track-icon) (cmd 40900))
(define (freeze-to-mono--render-pre-fader,-save-remove-items-and-online-fx-) (cmd 40901))
(define (insert-show-reacontrolmidi--midi-track-control-) (cmd 40907))
(define (vertical-scroll-selected-tracks-into-view) (cmd 40913))
(define (set-first-selected-track-as-last-touched-track) (cmd 40914))
(define (insert-multiple-new-tracks...) (cmd 41067))
(define (select-track-under-mouse) (cmd 41110))
(define (insert-new-track-at-end-of-mixer) (cmd 41147))
(define (set-track-solo-defeat) (cmd 41197))
(define (unset-track-solo-defeat) (cmd 41198))
(define (toggle-track-solo-defeat) (cmd 41199))
(define (freeze-to-stereo--render-pre-fader,-save-remove-items-and-online-fx-) (cmd 41223))
(define (lock-track-controls) (cmd 41312))
(define (unlock-track-controls) (cmd 41313))
(define (toggle-lock-unlock-track-controls) (cmd 41314))
(define (set-record-path-to-primary) (cmd 41321))
(define (set-record-path-to-secondary) (cmd 41322))
(define (set-record-path-to-primary+secondary) (cmd 41323))
(define (select-all-tracks-that-have-controls-locked) (cmd 41324))
(define (do-not-link-track-volume-pan-controls-to-midi-volume-pan) (cmd 41538))
(define (link-track-volume-pan-controls-to-midi-volume-pan-on-all-channels) (cmd 41555))
(define (toggle-link-unlink-track-volume-pan-controls-to-midi-volume-pan-on-all-channels) (cmd 41556))
(define (insert-new-surround-track) (cmd 41584))
(define (insert-new-surround-track,-using-selected-tracks-as-source-audio) (cmd 41585))
(define (toggle-tracks-visible--hide--in-tcp) (cmd 41591))
(define (toggle-tracks-visible--hide--in-mixer) (cmd 41592))
(define (hide-tracks-in-tcp-and-mixer) (cmd 41593))
(define (make-all-tracks-visible-in-tcp-and-mixer) (cmd 41594))
(define (unfreeze-tracks--restore-previously-saved-items-and-fx-) (cmd 41644))
(define (render-selected-area-of-tracks-to-stereo-post-fader-stem-tracks--and-mute-originals-) (cmd 41716))
(define (render-selected-area-of-tracks-to-multichannel-post-fader-stem-tracks--and-mute-originals-) (cmd 41717))
(define (render-selected-area-of-tracks-to-mono-post-fader-stem-tracks--and-mute-originals-) (cmd 41718))
(define (render-selected-area-of-tracks-to-stereo-stem-tracks--and-mute-originals-) (cmd 41719))
(define (render-selected-area-of-tracks-to-multichannel-stem-tracks--and-mute-originals-) (cmd 41720))
(define (render-selected-area-of-tracks-to-mono-stem-tracks--and-mute-originals-) (cmd 41721))
(define (toggle-full-multichannel-metering) (cmd 41726))
(define (set-track-record-mode-to-midi-latch-replace) (cmd 41727))
(define (toggle-track-metering) (cmd 41744))
(define (insert-show-reaeq--track-eq-) (cmd 41757))
(define (select-all-top-level-tracks) (cmd 41803))
(define (select-previous-envelope) (cmd 41863))
(define (select-next-envelope) (cmd 41864))
(define (select-pre-fx-volume-envelope) (cmd 41865))
(define (select-volume-envelope) (cmd 41866))
(define (select-pre-fx-pan-envelope) (cmd 41867))
(define (select-pan-envelope) (cmd 41868))
(define (select-pre-fx-width-envelope) (cmd 41869))
(define (select-width-envelope) (cmd 41870))
(define (select-mute-envelope) (cmd 41871))
(define (toggle-preserve-pdc-delayed-monitoring-in-recorded-items) (cmd 41919))
(define (unset-preserve-pdc-delayed-monitoring-in-recorded-items) (cmd 41920))
(define (set-preserve-pdc-delayed-monitoring-in-recorded-items) (cmd 41921))
(define (view-envelopes-for-current-last-touched-track-at-mouse-cursor) (cmd 41975))
(define (move-tracks-to-subproject) (cmd 41997))
(define (apply-trim-envelope-to-volume-envelope,-clear-trim-envelope) (cmd 42018))
(define (apply-volume-envelope-to-trim-envelope,-clear-volume-envelope) (cmd 42019))
(define (toggle-track-trim-envelope-visible) (cmd 42020))
(define (swap-volume-envelope-and-trim-envelope) (cmd 42021))
(define (toggle-midi-input-quantize-for-selected-tracks) (cmd 42033))
(define (toggle-midi-input-quantize-for-all-tracks) (cmd 42034))
(define (toggle-midi-input-quantize-for-last-touched-track) (cmd 42035))
(define (set-midi-input-quantize-to-grid-for-selected-tracks) (cmd 42060))
(define (set-midi-input-quantize-to-grid-for-all-tracks) (cmd 42061))
(define (set-midi-input-quantize-to-grid-for-last-touched-track) (cmd 42062))
(define (enable-midi-input-quantize-for-selected-tracks) (cmd 42063))
(define (disable-midi-input-quantize-for-selected-tracks) (cmd 42064))
(define (enable-midi-input-quantize-for-all-tracks) (cmd 42065))
(define (disable-midi-input-quantize-for-all-tracks) (cmd 42066))
(define (enable-midi-input-quantize-for-last-touched-track) (cmd 42067))
(define (disable-midi-input-quantize-for-last-touched-track) (cmd 42068))
(define (prevent-spectral-peaks-spectrogram) (cmd 42075))
(define (bypass-track-playback-offset) (cmd 42232))
(define (apply-playback-offset-to-receive-source-tracks) (cmd 42233))
(define (view-routing-and-i-o-for-master-track) (cmd 42235))
(define (lock-unlock-track-height) (cmd 42336))
(define (insert-track-from-template...) (cmd 46000))
